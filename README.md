# Healthcheck + run migrate

### Playground for AWS ECS healthcheck + and automigrate

The main reason of this project is FBV with `python manage.py migrate` functionality:
```
    url(r'^hcheck', health_check, name='hcheck'),
```
